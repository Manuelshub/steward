{
  "name": "@steward/agent",
  "version": "0.1.0",
  "private": true,
  "description": "The constrained-autonomy decision loop: signals -> LLM proposal -> compile -> submit -> audit.",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "test": "tsc -p tsconfig.json && node --test --test-reporter spec dist/*.test.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.68.0",
    "@steward/core": "*",
    "@steward/sdk": "*"
  },
  "devDependencies": {
    "@types/node": "^24.13.2",
    "typescript": "^5.9.3"
  }
}
